<h1>Комната <%= @room.id %></h1>

<%= link_to "Leave", root_path %>

<p id="notice"><%= notice %></p>

<div class="messages">
  <div class="messages-inner">
    <%= render @room.messages %>
  </div>
</div>
